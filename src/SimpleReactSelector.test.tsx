import SimpleReactSelector from "./SimpleReactSelector";
/*
describe("component", () => {
    test("toto", () => {
        expect(1).toBe(1)
    })
})

*/
// avec JEST
// voir que le trigger click fonctionne bien
// qu'il soit ouvert ou pas
// qu'une option du sélecteur soit bien présente ou pas dans le DOM